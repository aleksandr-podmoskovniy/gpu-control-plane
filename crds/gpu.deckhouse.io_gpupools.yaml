---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    module: gpu-control-plane
  name: gpupools.gpu.deckhouse.io
spec:
  group: gpu.deckhouse.io
  names:
    kind: GPUPool
    listKind: GPUPoolList
    plural: gpupools
    singular: gpupool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GPUPool defines a logical pool of GPU capacity exposed to workloads.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec declares desired rules for selecting and slicing devices.
            properties:
              backend:
                default: DevicePlugin
                description: Backend chooses integration backend (device-plugin or
                  DRA).
                enum:
                - DevicePlugin
                - DRA
                type: string
              deviceAssignment:
                description: DeviceAssignment controls manual vs automatic assignment
                  flows.
                properties:
                  autoApproveSelector:
                    description: AutoApproveSelector lists nodes/devices that may
                      be auto-approved.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  requireAnnotation:
                    description: RequireAnnotation forces manual approval before attaching
                      devices to the pool.
                    type: boolean
                type: object
              deviceSelector:
                description: DeviceSelector filters devices that may join the pool.
                properties:
                  exclude:
                    description: Exclude defines negative selection rules that remove
                      devices from the pool.
                    properties:
                      inventoryIDs:
                        description: InventoryIDs matches specific devices by inventory
                          identifier.
                        items:
                          type: string
                        type: array
                      migCapable:
                        description: MIGCapable restricts selection to devices that
                          are (or are not) MIG capable.
                        type: boolean
                      migProfiles:
                        description: MIGProfiles matches devices that support at least
                          one of the listed MIG profiles.
                        items:
                          type: string
                        type: array
                      pciDevices:
                        description: PCIDevices matches devices by PCI device identifier
                          (hex without 0x, e.g. 20b0).
                        items:
                          type: string
                        type: array
                      pciVendors:
                        description: PCIVendors matches devices by PCI vendor identifier
                          (hex without 0x, e.g. 10de).
                        items:
                          type: string
                        type: array
                      products:
                        description: Products matches devices by reported hardware
                          product name (exact match).
                        items:
                          type: string
                        type: array
                    type: object
                  include:
                    description: Include defines positive selection rules for devices.
                    properties:
                      inventoryIDs:
                        description: InventoryIDs matches specific devices by inventory
                          identifier.
                        items:
                          type: string
                        type: array
                      migCapable:
                        description: MIGCapable restricts selection to devices that
                          are (or are not) MIG capable.
                        type: boolean
                      migProfiles:
                        description: MIGProfiles matches devices that support at least
                          one of the listed MIG profiles.
                        items:
                          type: string
                        type: array
                      pciDevices:
                        description: PCIDevices matches devices by PCI device identifier
                          (hex without 0x, e.g. 20b0).
                        items:
                          type: string
                        type: array
                      pciVendors:
                        description: PCIVendors matches devices by PCI vendor identifier
                          (hex without 0x, e.g. 10de).
                        items:
                          type: string
                        type: array
                      products:
                        description: Products matches devices by reported hardware
                          product name (exact match).
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              nodeSelector:
                description: NodeSelector limits the pool to specific nodes.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              provider:
                default: Nvidia
                description: Provider selects GPU vendor implementation (only "Nvidia"
                  is supported for now).
                enum:
                - Nvidia
                type: string
              resource:
                description: Resource defines the resource unit exposed to workloads.
                  Resource name is derived from pool name.
                properties:
                  maxDevicesPerNode:
                    description: MaxDevicesPerNode caps number of devices contributed
                      per node.
                    format: int32
                    type: integer
                  migProfile:
                    description: MIGProfile specifies the MIG profile when Unit=MIG
                      (single profile shortcut).
                    type: string
                  slicesPerUnit:
                    default: 1
                    description: SlicesPerUnit configures oversubscription per base
                      unit (card or MIG partition).
                    format: int32
                    maximum: 64
                    minimum: 1
                    type: integer
                  unit:
                    description: Unit describes the resource unit (Card or MIG).
                    enum:
                    - Card
                    - MIG
                    type: string
                required:
                - unit
                type: object
              scheduling:
                description: Scheduling configures topology spreading, taints and
                  other scheduling hints.
                properties:
                  strategy:
                    description: Strategy controls balancing strategy among nodes
                      (BinPack or Spread).
                    enum:
                    - BinPack
                    - Spread
                    type: string
                  taints:
                    description: Taints enumerates taints applied to nodes that host
                      workloads from this pool.
                    items:
                      properties:
                        effect:
                          description: Effect is the Kubernetes taint effect.
                          type: string
                        key:
                          description: Key is the taint key applied to nodes hosting
                            the pool.
                          type: string
                        value:
                          description: Value is the taint value.
                          type: string
                      required:
                      - key
                      type: object
                    type: array
                  taintsEnabled:
                    description: TaintsEnabled controls whether per-pool taints/tolerations
                      are applied.
                    type: boolean
                  topologyKey:
                    description: TopologyKey configures topology spreading key when
                      strategy=Spread.
                    type: string
                type: object
            required:
            - resource
            type: object
          status:
            description: Status reports aggregated usage, candidates and conditions
              for the pool.
            properties:
              capacity:
                description: Capacity summarises total capacity inside the pool.
                properties:
                  total:
                    description: Total is total pool capacity expressed in declared
                      units.
                    format: int32
                    type: integer
                type: object
              conditions:
                description: Conditions surfaces pool-level status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
