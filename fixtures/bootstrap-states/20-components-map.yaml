# Structured state produced by bootstrap controller, covers all components.
gpuControlPlane:
  internal:
    bootstrap:
      components:
        validator:
          phase: Monitoring
          nodes:
            - worker-a
        gpu-feature-discovery:
          phase: Monitoring
          nodes:
            - worker-a
        dcgm:
          phase: Ready
          nodes:
            - worker-a
            - worker-b
        dcgm-exporter:
          phase: Ready
          nodes:
            - worker-a
        gpu-controller:
          phase: Ready
          nodes:
            - worker-a
        gpu-node-agent:
          phase: Ready
          nodes:
            - worker-a
        gpu-dra-controller:
          phase: Ready
          nodes:
            - worker-a
        gpu-dra-plugin:
          phase: Ready
          nodes:
            - worker-a
